import 'strings.dart';

/// The translations for English (`en`).
class WorldStringsEn extends WorldStrings {
  WorldStringsEn([String locale = 'en']) : super(locale);

  @override
  String get world => 'World!';
}
