import 'strings.dart';

/// The translations for Spanish Castilian (`es`).
class WorldStringsEs extends WorldStrings {
  WorldStringsEs([String locale = 'es']) : super(locale);

  @override
  String get world => 'Mundo!';
}
