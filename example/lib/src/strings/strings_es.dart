import 'strings.dart';

/// The translations for Spanish Castilian (`es`).
class MainStringsEs extends MainStrings {
  MainStringsEs([String locale = 'es']) : super(locale);

  @override
  String get title => 'Ejemplo';
}
