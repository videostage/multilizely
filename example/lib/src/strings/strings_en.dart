import 'strings.dart';

/// The translations for English (`en`).
class MainStringsEn extends MainStrings {
  MainStringsEn([String locale = 'en']) : super(locale);

  @override
  String get title => 'Example';
}
